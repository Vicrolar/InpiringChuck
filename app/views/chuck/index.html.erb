<h1>Chuck</h1>

<p>
    <%= link_to 'English', root_path(locale: :en)%>
    <%= link_to 'Español', root_path(locale: :es)%>
</p>

<%= form_with(url: search_path, method: 'get', local: true) do %>

    <div class='form-group'>

        <p>
            <%= radio_button_tag 'type', 'random', true %>
            <%= label_tag 'type_random', t('random') %>
            </br>
            
            <%= radio_button_tag 'type', 'words' %>
            <%= label_tag 'type_words', t('words') %>
            <%= text_field_tag 'txtWord'%>
            </br>
            
            <%= radio_button_tag 'type', 'category' %>
            <%= label_tag 'type_category', t('category') %>
            <%= select_tag "selCategory", options_for_select(@categories)%>
            </br>
            
            <%= label_tag t('email')%>
            <%= text_field_tag 'txtEmail'%>
            </br>
        </p>
    
    </div>


    <%= label_tag params[:error]%>
    </br>
    <%= button_tag t('search'), type: 'search' %>
<% end %>
